services:
  web:
    build: .
    container_name: web-llm-app
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - .:/app
    command: uvicorn backend:app --host 0.0.0.0 --port 8000 --reload
